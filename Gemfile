source "https://rubygems.org"

# Manage which Jekyll version is used to run.
# Want to use a different version? Change it below, save the
# file and run `bundle install`. Run Jekyll with `bundle exec`:
#
#     bundle exec jekyll serve
#

# If you have any plugins, put them here!
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
group :jekyll_plugins do
    gem 'jekyll-feed'
    gem 'jekyll-sitemap'
    gem 'jekyll-paginate'
    gem 'jekyll-seo-tag'
    gem 'jekyll-archives'
    gem 'kramdown'
    gem 'rouge'
    gem "github-pages"
    gem "jekyll-github-metadata"
end
